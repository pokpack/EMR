{
  "name": "health-care-blockchain",
  "version": "0.0.0",
  "private": true,
  "description": "",
  "main": "server.js",
  "scripts": {
    "build": "rimraf dist/ && babel ./ --out-dir dist/ --ignore ./node_modules,./.babelrc,./package.json,./npm-debug.log --copy-files",
    "start": "npm run build && node dist/server.js"
  },
  "dependencies": {
    "body-parser": "^1.15.2",
    "crypto-js": "^3.1.6",
    "express": "^4.17.1",
    "morgan": "^1.8.1",
    "nodemon": "^2.0.6",
    "rimraf": "^2.6.1",
    "rimraff": "0.0.1-security",
    "ws": "^1.1.0"
  },
  "engines": {
    "node": ">=8"
  },
  "devDependencies": {
    "babel-cli": "^6.24.0",
    "babel-preset-es2015": "^6.24.0"
  }
}
